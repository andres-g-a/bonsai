// Copiá este archivo a `config.js` y completá las credenciales.
// `config.js` está en .gitignore — no se commitea al repo.
//
// 1. Get API key: https://aistudio.google.com/ → "Get API key" → "Create API key"
// 2. Pegá la key abajo
// 3. Recargá el browser
//
// Si no completás esto, la app funciona igual en modo demo (mock data, no LLM).

window.APP_CONFIG = {
  GEMINI_API_KEY: "",                  // Pegá tu key acá (formato: AIzaSy...)
  GEMINI_MODEL: "gemini-2.0-flash"     // o "gemini-2.5-flash" si tenés acceso
};
